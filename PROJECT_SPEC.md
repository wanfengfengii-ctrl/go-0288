# 深水承台钻孔灌注桩灌注轨迹与完整性验收闭环

## 项目目标

建设一个面向桥梁深水承台钻孔灌注桩的 Go HTTP 后端。系统以锁定后的桩基设计快照为唯一判定基准，依次管理终孔复测、清孔、钢筋笼下放、声测管布置、导管拼接与水密、首灌封底、连续灌注、导管拆节、桩顶收面、龄期等待、完整性检测、异常复检和双人验收。核心账本是按“桩孔深度区间—逻辑时点”保存的只追加轨迹；SQLite 持久化设计快照、材料余额、设备租约、证据代次、幂等记录和终局凭据，服务重启后可恢复未完成任务、租约及待重试设备调用。实现规模限定为约 20—28 个生产 Go 文件、2000—3000 行有效生产代码，目标约 2500 行，分布于不少于 4 个有实际职责的包；公开测试不少于 4 个测试文件和 12 个确定性用例。

## 端到端业务流程

1. 创建成桩任务并锁定墩位、桩号、设计孔深与桩径、地层深度分段、钢筋笼及声测管布置、泥浆阈值、混凝土批次、导管节段、首灌方量、连续浇筑时限、超灌高度、检测测线邻接表、抽芯规则和任务代次；锁定后业务规则不可修改。
2. 按锁定点位提交终孔深度、孔径抽测、沉渣厚度以及泥浆比重、黏度、含砂率；所有点位完整且定点计算合格后，才能确认清孔验收。随后提交钢筋笼节段、接头和声测管实际深度覆盖，整体校验连续性、方向与位置。
3. 登记导管节段拼接顺序、长度和水密结果；全部通过后，以操作号原子获取混凝土泵、测绳或记录器租约并从指定批次扣减首灌整数升方量，计算封底液面及导管埋深，满足首灌阈值方可开始连续灌注。
4. 后续灌注、液面复测和导管拆节按严格递增逻辑时点写入只追加轨迹。每次灌注原子扣减批次余额，累计方量与理论占容单调增加；拆节只能从导管顶部依次移除，使在用导管始终为原拼接序列的连续前缀，并确保底口埋深处于锁定区间。
5. 达到锁定超灌高度后收面并记录完成逻辑时点。达到规定龄期后，按锁定测线执行声波透射；断灌、方量突变、缩颈或夹泥疑点、声速异常及波幅衰减根据地层深度邻接和相邻测线交叉规则，一次性生成排序稳定的复检范围，必要时分配取芯任务。
6. 复检始终创建新检测代次并保留原始证据；旧代次迟到回执仅归档，不得改变当前结论。全部证据闭合后，由两名不同且合格的人员独立复核；验收、质量隔离和取消通过单写终局屏障竞争，唯一胜者生成不可重复的终局记录。

## 核心组件与职责

1. 桩基设计与材料规则目录：校验并锁定设计摘要、深度分段、构造布置、材料批次、定点阈值、设备需求、检测邻接表及复检规则，提供不可变版本快照。
2. 成桩任务及深度轨迹聚合：实现流程状态机和只追加深度轨迹，维护逻辑时点、累计方量、理论液面、实测液面、导管前缀、埋深、超灌高度与重启恢复。
3. 混凝土方量与设备租约管理器：管理整数升批次池、预留和扣减事务，以及混凝土泵、测绳、泥浆仪、声测仪、取芯机等资源的限时互斥租约、续约、释放和过期回收。
4. 清孔导管及连续灌注证据记录器：验证终孔与泥浆样点、钢筋笼及声测管覆盖、导管拼接和水密，编排设备调用，记录首灌、连续灌注、拆节和设备失败的不可变证据。
5. 完整性复检与终局仲裁器：维护检测代次和原始证据链，确定性生成复检集合，隔离迟到回执，校验双人复核资格，并以单写屏障裁定验收、质量隔离或取消。
6. Go HTTP API：提供 JSON 接口、稳定错误码、幂等操作处理、事务边界、确定性排序和健康检查；启动时执行数据库迁移并恢复未完成业务状态。

## 领域规则与不变量

1. 墩位、桩号与设计摘要必须一致；任务锁定后，设计孔深、桩径、地层分段、钢筋笼、声测管、批次、导管、阈值、窗口、测线、复检规则和任务代次均不可替换。
2. 所有长度使用整数毫米、体积使用整数升、比重和百分率使用约定比例的整数定点值、时间使用单调递增逻辑时点；统一执行指定舍入、除零、负数以及加减乘换算溢出检查，禁止浮点参与裁定。
3. 终孔、孔径、沉渣和泥浆样点必须完整覆盖锁定点位。钢筋笼节段、接头及声测管必须无重复、无缺段、无交叠且方向正确；导管总长须与实测孔深相容且水密全部通过，组合验证失败时整体拒绝。
4. 灌注轨迹只能追加。累计方量和理论占容不得回退；液面换算须使用锁定桩径及深度分段规则。首灌必须达到锁定封底方量，连续灌注相邻事件不得超过锁定时限。
5. 导管拆节只允许移除当前在用序列的末端节段，形成原拼接序列的连续前缀；每次灌注、复测和拆节后，导管底口必须位于已灌混凝土面以下并处于锁定最小、最大埋深之间。
6. 混凝土扣减、轨迹追加和所需租约确认属于同一事务；余额不足、租约冲突或过期、设备失败以及计算错误均不得留下部分扣减、租约、轨迹前缀或有效证据。相同操作号与相同规范化内容返回原结果，不同内容返回幂等冲突。
7. 外部设备拒绝、断连、超时或格式错误只生成带确定重试次数和下一逻辑时点的待重试调用，不产生读数或推进状态；超过锁定重试次数后形成质量异常。
8. 异常范围按锁定地层分段边界扩展，并合并相交或相邻区间；声测异常同时纳入异常测线及其邻接测线。结果按墩位、桩号、区间起止、逻辑时点、导管节段和测线排序。复检使用新代次，旧代次证据不可覆盖；两名不同合格复核人通过且所有闭环条件成立后才能竞争唯一终局。

## 数据模型与持久化

1. DesignSnapshot：任务代次、墩位、桩号、设计摘要、孔深、桩径、地层分段、钢筋笼与声测管方案、泥浆阈值、灌注窗口、超灌高度、测线邻接和抽芯规则。
2. PileTask：任务标识、当前阶段、锁定版本、当前检测代次、最后逻辑时点、龄期截止时点、版本号和终局状态。
3. InspectionEvidence：证据类型、点位或深度区间、整数测值、设备调用标识、逻辑时点、代次、原始载荷摘要及有效性状态。
4. ConduitAssembly：有序导管节段、各节长度、拼接方向、水密证据、当前有效前缀长度和底口深度。
5. PourTraceEntry：只追加序号、操作号、逻辑时点、事件类型、批次扣减升数、累计升数、理论与实测液面、导管前缀、埋深及超灌高度。
6. ConcreteBatch与DeviceLease：批次初始量、已扣减量、可用量；设备类型、资源号、持有任务、租约令牌、起止逻辑时点和状态。
7. RetryCall与ReviewGeneration：设备请求、尝试次数、下次重试时点、失败码；检测代次、测线证据、异常区间、复检集合、父代次和当前结论。
8. IdempotencyRecord、ReviewerDecision与TerminalRecord：操作号及请求摘要、保存结果；人员资格快照和独立复核；唯一终局类型、凭据号、裁定依据与提交版本。

## 公开接口

1. POST /v1/piles：创建任务；POST /v1/piles/{id}/lock：锁定设计及规则快照。
2. POST /v1/piles/{id}/borehole-checks、/cleaning-acceptance、/cages、/conduits：提交终孔清孔、钢筋笼声测管、导管拼接与水密证据。
3. POST /v1/piles/{id}/pour/start、/pour/entries、/pour/level-readings、/pour/remove-segments、/pour/finish：执行首灌、连续灌注、液面复测、拆节和收面；所有变更接口要求 Idempotency-Key 与逻辑时点。
4. POST /v1/piles/{id}/device-calls/{callID}/retry：按脚本化设备结果重试；GET /v1/piles/{id}/leases 与 /batches/{batchID}：查询租约和方量守恒。
5. POST /v1/piles/{id}/integrity-results、/review-generations、/core-results：登记完整性检测、生成复检代次和提交取芯结果；GET /v1/piles/{id}/evidence：按规定字段稳定排序返回证据链。
6. POST /v1/piles/{id}/reviews 与 /terminal-decisions：提交独立复核并竞争验收、质量隔离或取消；GET /v1/piles/{id}、/trace、/terminal：查询任务、轨迹与唯一终局；GET /healthz：返回服务和持久化状态。

## 失败边界

1. 请求级数据库事务覆盖批次余额校验与扣减、租约确认、轨迹追加、证据生效及幂等结果保存；任一检查失败全部回滚。SQLite 使用唯一约束、外键、检查约束和乐观版本号阻止重复节段、重复操作、代次冲突及并发覆盖。
2. 稳定错误码包括 DESIGN_MISMATCH、BOREHOLE_CONFLICT、SAMPLE_MISSING、REBAR_LAYOUT_INVALID、CONDUIT_DUPLICATE、CONCRETE_INSUFFICIENT、LEASE_CONFLICT、FIRST_POUR_INSUFFICIENT、EMBEDMENT_OUT_OF_RANGE、POUR_INTERRUPTED、FIXED_POINT_OVERFLOW、DEVICE_REJECTED、DEVICE_TIMEOUT、GENERATION_CONFLICT、IDEMPOTENCY_CONFLICT 与 TERMINAL_ALREADY_DECIDED。
3. 所有多原因拒绝均规范化并按墩位、桩号、深度区间、逻辑时点、导管节段和测线排序；同一输入在不同运行和重启后产生相同状态码、错误码及原因顺序。
4. 租约以逻辑时点而非墙钟参与公共测试；过期租约只能由事务内的确定性回收流程转为失效。服务崩溃重启后从持久化账本恢复余额、有效租约、轨迹序号、待重试调用、当前检测代次和终局。
5. 设备适配器只返回成功读数或明确故障。故障事务不创建伪造读数；迟到回执先校验任务、调用尝试与检测代次，不匹配时仅作不可生效归档。
6. 终局表以任务唯一键和版本比较实现单写屏障；验收、隔离、取消并发提交时恰有一个成功，其余返回同一既有终局，不产生额外凭据。

## 验收标准

1. 锁定操作完整固化桩基设计摘要、孔深桩径、地层、钢筋笼与声测管、混凝土批次、导管节段、清孔阈值、灌注窗口、测线邻接、复检规则和任务代次；错配或锁定后修改以稳定错误码拒绝。
2. 终孔、孔径、沉渣和三类泥浆指标完整覆盖锁定点位并通过整数定点校验后方可推进；缺样、不合格、除零或溢出均保持原状态。
3. 钢筋笼、接头和声测管形成无重复的连续覆盖，导管长度与实测孔深相容且水密合格；缺段、交叠、反向、重复或水密失败整体回滚。
4. 首灌和后续灌注原子扣减整数升混凝土并确认设备租约；幂等重试返回原结果，余额不足、租约冲突、过期或内容冲突不遗留任何部分状态。
5. 只追加轨迹按逻辑时点推进，累计方量、理论液面、实测约束、埋深、拆节前缀、连续浇筑边界和桩顶超灌均以整数规则正确裁定，符号或乘加溢出被拒绝。
6. 脚本化设备拒绝、断连、超时和格式错误仅产生确定的待重试记录；异常依据锁定深度分段及测线邻接一次性生成排序稳定的复检集合，新代次隔离旧代次迟到证据。
7. 清孔、构造、导管、方量守恒、连续轨迹、龄期和完整性证据全部闭合且异常复检完成后，两名不同合格人员独立通过才能生成唯一验收凭据。
8. 服务重启可恢复任务、方量、租约、轨迹、待重试和检测代次；同步并发的验收、质量隔离与取消只能产生一个终局。仓库包含 go.mod、可编译 Go 后端及同时支持 linux/amd64 和 linux/arm64 的 Docker 构建配置，生产规模保持 20—28 个 Go 文件、2000—3000 行。

## 确定性测试场景

1. 在固定桥墩、桩号、孔深、桩径和设计摘要下成功锁定；验证摘要错配、重复锁定和锁定后规则变更。
2. 用公开地层分段和采样点验证终孔复测冲突、泥浆样点缺失、阈值边界舍入、除零及毫米—升换算溢出。
3. 验证钢筋笼与声测管连续覆盖成功，以及缺段、交叠、反向、错位和重复节段的整体拒绝。
4. 验证导管拼接长度、水密和重复节段；多次合法拆节保持连续前缀，跨段拆除及导致埋深越界的拆节被拒绝。
5. 两个 goroutine 通过同步屏障并发超领同一混凝土批次，断言仅一个事务成功且余额守恒；并发争用同一泵租约得到确定胜者。
6. 验证首灌不足不扣减余额、不保留租约或轨迹；成功首灌及后续灌注的累计升数、理论液面、埋深和超灌高度精确一致。
7. 在连续浇筑时限的等值边界成功、超出一个逻辑时点失败；验证时间倒退、方量突变、负值和乘加溢出。
8. 相同幂等键同内容并发重试只产生一条轨迹，相同键不同内容返回 IDEMPOTENCY_CONFLICT，服务重启后仍保持相同结果。
9. 脚本化泥浆仪、测绳适配器和泵送记录器依次返回拒绝、超时、格式错误与成功，断言重试次数、下一逻辑时点及状态不提前推进。
10. 依据固定地层邻接和测线邻接表输入断灌、缩颈及声波异常，精确断言合并后的复检深度区间、测线集合和稳定排序。
11. 创建第二检测代次后提交第一代次迟到声测回执，断言原证据保留但当前结论不变；验证代次号冲突和取芯设备租约冲突。
12. 先后及并发提交双人复核，验证同一人员不能重复充当两名复核人、证据未闭合不能验收；用三方同步屏障竞争验收、质量隔离和取消，精确断言唯一终局。
13. 在批次扣减与轨迹写入之间注入事务故障，重启服务后断言余额、租约、轨迹、有效证据和凭据均无部分遗留。

## 组件追踪关系

1. 设计与规则目录对应锁定流程、定点阈值和摘要错配处理，主要覆盖验收标准 1—3。
2. 任务及深度轨迹聚合对应只追加逻辑时点、方量与液面单调性、导管埋深、拆节前缀、连续浇筑及超灌，主要覆盖验收标准 5。
3. 方量与设备租约管理器对应批次守恒、并发超领、互斥租约、超时回收和事务回滚，主要覆盖验收标准 4、6。
4. 清孔导管及灌注证据记录器对应终孔清孔、构造覆盖、水密、首灌封底、设备失败和不可变原始证据，主要覆盖验收标准 2—6。
5. 完整性复检与终局仲裁器对应异常传播、复检集合、检测代次隔离、双人复核和单写终局，主要覆盖验收标准 7—8。
6. HTTP API 统一承载幂等键、稳定错误码、确定性排序、持久化事务和重启恢复，使全部业务能力可由公开黑盒测试复现。

## 独特性

本项目聚焦深水承台钻孔灌注桩的“深度区间—逻辑时点”灌注轨迹，把整数升混凝土守恒、导管连续前缀与动态埋深、测线邻接复检代次以及三类竞争终局组合为同一质量闭环；其核心对象、状态约束和故障模型均不同于既有材料入场联检或构件放行项目。
